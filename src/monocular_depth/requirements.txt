# Installed into src/tk26_vision/.venv-da3 (NOT the shared .venv-vision-main).
# Lock file at src/tk26_vision/.venv-da3/freeze.lock.txt.
#
# DA3 itself is editable from thirdparty/:
#   pip install -e ../../thirdparty/depth-anything-3 --no-deps
#
# Forced version pins:
numpy==1.23.4
opencv-python==4.7.0.72
imageio==2.31.5
scipy==1.10.1

# Inference-path deps (skipping pycolmap/open3d/moviepy/trimesh/plyfile/
# pillow_heif/uvicorn/fastapi/typer/xformers — DA3 export pipeline only):
einops
jaxtyping
omegaconf
safetensors
huggingface_hub
pillow
addict
evo
