# Runtime Python deps for pan_tilt nodes.
# Install into the Python environment that `ros2 run` uses:
#   python3 -m pip install -r src/tk26_vision/src/pan_tilt/requirements.txt
pyserial>=3.5
ultralytics>=8.0
opencv-python>=4.5
numpy>=1.21
# Extrinsic calibration (calibrate_collect + run_calibration):
scipy>=1.10
PyYAML>=5.0
