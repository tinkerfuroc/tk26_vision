# YOLO Object Tracking with Segmentation - Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
numpy>=1.21.0
opencv-python>=4.5.0
scipy>=1.7.0

# PyTorch - For GPU support, visit https://pytorch.org/get-started/locally/
# for installation instructions specific to your CUDA version.
# The command below installs CPU version. For GPU, use:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
torch>=2.0.0
torchvision>=0.15.0

# Ultralytics YOLO - For object detection and segmentation
ultralytics>=8.0.0

# For ByteTrack
lap>=0.4.0

# Person ReID backbone (OSNet) — pretrained weights via torchreid.
# The PyPI 'torchreid' wheel is 0.2.5 (namespaces models/utils under
# torchreid.reid.*); gdown pulls OSNet weights, and tensorboard is a runtime
# import dep of torchreid's __init__ (it loads the training engine). All three
# installed additively into .venv-vision-main with zero version churn
# (numpy stayed 1.26.4, torch stayed 2.11.0+cu128). See src/tk26_vision/CLAUDE.md.
torchreid==0.2.5
gdown>=4.0.0
tensorboard>=2.0.0

# track_web dashboard — ROS-free FastAPI app + test client (Task 5)
fastapi
uvicorn
httpx
